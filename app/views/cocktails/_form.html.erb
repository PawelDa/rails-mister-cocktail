<%= hidden_field_tag :authenticity_token, form_authenticity_token %>
<div class="form">
  <%= simple_form_for @cocktail do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <div id="add-button-container"><%= f.submit "Update", class: "add-button"%></div>
  <% end %>
</div>
